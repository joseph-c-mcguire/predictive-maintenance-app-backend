
build:
  docker:
    frontend: ./predictive-maintenance/Dockerfile
    backend: ./back-end/Dockerfile

release:
  docker:
    dockerfile: ./predictive-maintenance/Dockerfile
    dockerfile: ./back-end/Dockerfile

process_types:
  web: docker-compose up -d  # If you're using docker-compose